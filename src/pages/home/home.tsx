import { Box, Collapsible, pxRem, Text } from '../../lib';
import React, { FC, useState } from 'react';
import styled from 'styled-components';

export const HomePage: FC = () => {
    return (
        <Text fz={25} md={{ fz: 40, $color: 'red' }}>
            kek
        </Text>
    );
};
